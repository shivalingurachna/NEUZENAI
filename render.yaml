services:
  # 1. Backend Express API Web Service
  - type: web
    name: neuzenai-server
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node src/app.js
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://shivalingurachana461_db_user:rachana%40123neuzenai@r55alrw.mongodb.net/neuzenai_hrms?retryWrites=true&w=majority&appName=NEUZENAI
      - key: JWT_SECRET
        value: neuzenai_super_secret_production_key_2026
      - key: NODE_ENV
        value: production

  # 2. Frontend React Client Static Site
  - type: web
    name: neuzenai-client
    env: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://neuzenai-server.onrender.com
